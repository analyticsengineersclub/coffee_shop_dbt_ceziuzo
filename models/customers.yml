version: 2

models:
  - name: customers
    description: 'cleaned up table of customers'
    columns:
      - name: customer_id
        description: Primary key of the model
        tests:
          - unique
          - not_null